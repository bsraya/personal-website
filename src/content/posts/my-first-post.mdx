---
title: "My First Post"
description: "This is my very first post"
publishedDate: 2024-05-03
tag: First
series:
published: false
---

# Introduction

I run the following code `print("something")`.

```js title="app.js" {1, 4}
// Javascript code with syntax highlighting.
var fun = function lang(l) {
  dateformat.i18n = require('./lang/' + l);
  return true;
};
```

Some paragraph in-between two codeblocks.

```python showLineNumbers=false
def fibonacci(number: int) -> list(int):
  pass

if __name__ == "__main__":
  print("Hello, world!")
  print(fibonnaci(number=10))
```

$$$
\alpha = \beta + 1
$$$

```bash showLineNumbers=false withOutput
> def print(test: str) -> null:
>   print(f"{test}")

/usr/home/boba-tan/programming
```