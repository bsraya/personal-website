---
import BaseLayout from "./BaseLayout.astro";
const { frontmatter } = Astro.props;
---

<BaseLayout pageTitle={frontmatter.pageTitle}>
  <div>Title: {frontmatter.title}</div>
  <p>Published on : {frontmatter.publishedDate}</p>
  <slot />
</BaseLayout>
